requests>=2.28.0
beautifulsoup4>=4.12.0
pandas>=2.0.0
python-dotenv>=1.0.0
gspread>=5.11.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Optional: AI job parsing (set OPENAI_API_KEY in .env to enable)
openai>=1.0.0

# Optional: LinkedIn scraping (set LINKEDIN_ENABLED=true in .env to enable)
# Also requires Chrome + matching chromedriver in PATH
linkedin_jobs_scraper>=2.3.0

# selenium is only needed if you use daily_job_automation.py (legacy file)
selenium>=4.15.0
